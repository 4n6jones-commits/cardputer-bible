# Build output, regenerated by build.sh
build/

# Byte-identical duplicate of sd-card-bible/bible/, produced as an
# intermediate step by prepare_bible.py - no need to track both copies
bible_prepared/
